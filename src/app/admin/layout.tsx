// Layout racine admin minimal afin de laisser les sous-groupes gérer leur propre layout
export default function AdminRootLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
